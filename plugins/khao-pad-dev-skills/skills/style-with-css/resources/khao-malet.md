# @der-reiskoch/khao-malet

## About

"Malet Khao" (เมล็ดข้าว) is Thai and means "grain(s) of rice". As this design system is the base of the component library "Khao UI" we used the thai name in reverse order.

Visually, the Khao Malet system follows the current design of Der Reiskoch. The design system is based on Material 3.

## Design tokens

Token naming is based on Material Web. All tokens are prefixed with `--khao-` to prevent naming clashes.

### Types of tokens

#### Reference tokens

- prefixed with `--khao-ref-`
- only assigned to system tokens, never used in components directly

#### System tokens

- prefixed with `--khao-sys-`

#### Component tokens

- prefixed with `--khao-<component-name>-`, e.g. --khao-button-

## Sources

- [npm](https://www.npmjs.com/package/@der-reiskoch/khao-malet)
- [github](https://github.com/Khao-UI-Tools/khao-ui) (Monorepo together with Khao-Ui)
- [Storybook](https://khao-ui-tools.github.io/khao-ui/storybook/?path=/docs/about-khao-ui--docs)
- If installed: `@der-reiskoch/khao-malet` in the local `node_modules` folder
